# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:Terminus 12

# only change focus with keyboard
focus_follows_mouse no

# Use pactl to adjust volume in PulseAudio.
bindsym XF86AudioRaiseVolume exec --no-startup-id ~/bin/volume -x 100 -P -e 1000 -p -n up 10
bindsym XF86AudioLowerVolume exec --no-startup-id  ~/bin/volume -x 100 -P -e 1000 -p -n down 10
bindsym XF86AudioMute exec --no-startup-id  ~/bin/volume -x 100 -P -e 1000 -p -n mute
#bindsym XF86AudioMicMute exec --no-startup-id
bindsym XF86MonBrightnessDown exec --no-startup-id ~/bin/backlight.sh -
bindsym XF86MonBrightnessUp exec --no-startup-id ~/bin/backlight.sh +
bindsym $mod+p exec --no-startup-id test "$(autorandr --current)" = "common" && autorandr -c || autorandr -l common

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec xterm

# kill focused window
bindsym $mod+Shift+k kill

# run program
bindsym $mod+d exec "rofi -modi drun,run -show drun"
bindsym $mod+l exec "loginctl lock-session"

# change focus
bindsym $mod+Left focus left
bindsym $mod+Prior focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right
bindsym $mod+Next focus right

# move focused window
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Prior move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right
bindsym $mod+Shift+Next move right

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
workspace_layout tabbed
bindsym $mod+z layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle
bindsym $mod+Shift+at move scratchpad
bindsym $mod+at scratchpad show

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1 "1:term"
set $ws2 "2:www-6WIND"
set $ws3 "3:www-perso"
set $ws4 "4:signal"
set $ws5 "5:spotify"
set $ws6 "6:email"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

# switch to workspace
bindsym $mod+ampersand workspace number $ws1
bindsym $mod+eacute workspace number $ws2
bindsym $mod+quotedbl workspace number $ws3
bindsym $mod+apostrophe workspace number $ws4
bindsym $mod+parenleft workspace number $ws5
bindsym $mod+minus workspace number $ws6
bindsym $mod+egrave workspace number $ws7
bindsym $mod+underscore workspace number $ws8
bindsym $mod+ccedilla workspace number $ws9
bindsym $mod+agrave workspace number $ws10
bindsym Ctrl+$mod+Left workspace prev
bindsym $mod+Delete workspace prev
bindsym Ctrl+$mod+Right workspace next
bindsym $mod+End workspace next

# move focused container to workspace
bindsym $mod+Shift+ampersand move container to workspace number $ws1
bindsym $mod+Shift+eacute move container to workspace number $ws2
bindsym $mod+Shift+quotedbl move container to workspace number $ws3
bindsym $mod+Shift+apostrophe move container to workspace number $ws4
bindsym $mod+Shift+parenleft move container to workspace number $ws5
bindsym $mod+Shift+minus move container to workspace number $ws6
bindsym $mod+Shift+egrave move container to workspace number $ws7
bindsym $mod+Shift+underscore move container to workspace number $ws8
bindsym $mod+Shift+ccedilla move container to workspace number $ws9
bindsym $mod+Shift+agrave move container to workspace number $ws10

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart

set $exit "EXIT i3? [y/N]"
mode $exit {
	bindsym y exit
	bindsym n mode "default"
	bindsym q mode "default"
	bindsym Space mode "default"
	bindsym Escape mode "default"
	bindsym Return mode "default"
}
bindsym $mod+Shift+e mode $exit

# resize window (you can also use the mouse for that)
mode "resize" {
	# These bindings trigger as soon as you enter the resize mode

	# Pressing left will shrink the window’s width.
	# Pressing right will grow the window’s width.
	# Pressing up will shrink the window’s height.
	# Pressing down will grow the window’s height.
	bindsym Left resize shrink width 10 px or 10 ppt
	bindsym Down resize grow height 10 px or 10 ppt
	bindsym Up resize shrink height 10 px or 10 ppt
	bindsym Right resize grow width 10 px or 10 ppt

	# back to normal: Enter or Escape or $mod+r
	bindsym Return mode "default"
	bindsym Escape mode "default"
	bindsym $mod+r mode "default"
}

bindsym $mod+r mode "resize"

# restart status bar every time i3 is restarted
exec_always --no-startup-id ~/.config/polybar/launch.sh

# change desktop background
exec_always --no-startup-id feh --image-bg '#0e1624' --bg-center ~/.wallpaper.png

# xss-lock grabs a logind suspend inhibit lock and will use i3lock to lock the
# screen before suspend. Use loginctl lock-session to lock screen.
exec --no-startup-id xss-lock --transfer-sleep-lock -- ~/.config/i3/lock.sh

# add network manager applet in system tray
exec --no-startup-id nm-applet

# monitor input devices activity to apply settings
exec --no-startup-id inputplug -0 -c ~/bin/inputplug.sh

# automatically update screen backlight when AC is plugged/unplugged
exec --no-startup-id ~/bin/ac-auto-backlight.sh

for_window [class="Hexchat"] move scratchpad
for_window [class="Hexchat"] sticky enable
for_window [class="Google-chrome"] move container to workspace number $ws2
for_window [class="Firefox"] move container to workspace number $ws3
for_window [class="Signal"] move container to workspace number $ws4
for_window [class="Spotify"] move container to workspace number $ws5
for_window [instance="aerc"] move container to workspace number $ws6
exec --no-startup-id xterm
exec --no-startup-id hexchat
exec --no-startup-id google-chrome
exec --no-startup-id firefox-esr
exec --no-startup-id signal-desktop
exec --no-startup-id spotify
exec --no-startup-id xterm -name aerc -e aerc
